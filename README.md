# All-Pairs-Shortest-Path
Brute Force (running Dijkstra from every node) vs. Floyd-Warshall (Dynamic Programming)** Both solve the same problem — shortest paths between every pair of nodes — but are structurally very different. 
